// converting the string to an array

const stringToArray = (string) => {
  const result = string.split(" ");
  console.log(result);
};
stringToArray("hello"); // ["hello"]
stringToArray("hello world"); // ["hello"]
