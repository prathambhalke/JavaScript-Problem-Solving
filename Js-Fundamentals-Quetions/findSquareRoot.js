// JavaScript Program to Find the Square Root

const findSquaRoot = (number) => {
  return Math.sqrt(number);
};

console.log(findSquaRoot(2.25)); // Expected 1.5
console.log(findSquaRoot(-4)); // Expected NaN
console.log(findSquaRoot("hello")); //Expected NaN
